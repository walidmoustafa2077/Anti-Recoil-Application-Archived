<UserControl x:Class="Anti_Recoil_Application.UserControls.LoginUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Anti_Recoil_Application.UserControls"
             xmlns:helpers="clr-namespace:Anti_Recoil_Application.Helpers"
             mc:Ignorable="d">
    <Grid>
        <StackPanel Width="400"
                    Height="auto"
                    VerticalAlignment="Center">
            <!-- List of Weapon Charts -->
            <StackPanel Margin="8">
                <ContentControl Style="{StaticResource BrandIcon}"
                                Width="auto"
                                Height="auto"
                                Foreground="{StaticResource BrandAccentBrush}"
                                RenderTransformOrigin="0.5,0.5" />

                <ContentControl Margin="0 12"
                                Style="{StaticResource BrandNameIcon}"
                                Width="auto"
                                Height="auto"
                                Foreground="{StaticResource BrandAccentBrush}"
                                RenderTransformOrigin="0.5,0.5" />
            </StackPanel>

            <StackPanel Margin="18 8 18 0"
                        x:Name="MyContainer">
                <!-- Primary Input Field -->
                <TextBox Style="{StaticResource PrimaryInputFieldStyle}"
                         helpers:WatermarkHelper.WatermarkText="Enter your username"
                         Margin="0,10,0,0"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

                <!-- Secondary Input Field -->
                <PasswordBox Style="{StaticResource NewPasswordBoxStyle}"
                             helpers:WatermarkHelper.WatermarkText="Enter your password"
                             Margin="0,10,0,0"
                             PasswordChanged="PasswordBox_PasswordChanged" />

                <!-- Text Button -->
                <Button Content="Forgot Password?"
                        Style="{StaticResource TextButtonStyle}"
                        HorizontalAlignment="Right"
                        Margin="0,6,0,0"
                        Command="{Binding ForgotPasswordCommand}" />
            </StackPanel>

            <StackPanel Margin="18 8 18 18">
                <!-- Primary Button -->
                <Button Content="Login"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,25,0,0"
                        Command="{Binding LoginCommand}" />

                <!-- Secondary Button -->
                <Button Content="Register"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,10,0,0"
                        Command="{Binding RegisterCommand}" />
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
