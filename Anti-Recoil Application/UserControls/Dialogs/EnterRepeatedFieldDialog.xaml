<UserControl x:Class="Anti_Recoil_Application.UserControls.Dialogs.EnterRepeatedFieldDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Anti_Recoil_Application.UserControls.Dialogs"
             xmlns:helpers="clr-namespace:Anti_Recoil_Application.Helpers"
             mc:Ignorable="d">

    <Border CornerRadius="10"
            BorderBrush="{StaticResource BrushTertiary}"
            BorderThickness="1.5"
            Background="{StaticResource BrushPrimary}"
            Width="Auto"
            MinWidth="256"
            Height="Auto">

        <Grid Margin="10">
            <StackPanel>
                <!-- Header -->
                <TextBlock x:Name="HeaderText"
                           Margin="12"
                           HorizontalAlignment="Center"
                           Text="{Binding HeaderText}"
                           FontWeight="Bold"
                           Style="{StaticResource LabelStyle}" />

                <!-- Username -->
                <PasswordBox x:Name="Password"
                             Style="{StaticResource NewPasswordBoxStyle}"
                             helpers:WatermarkHelper.WatermarkText="Enter your Password"
                             Margin="10 6 10 12"
                             PasswordChanged="PasswordBox_PasswordChanged" />
                <!-- Username -->
                <PasswordBox x:Name="RetypedPassword"
                    Style="{StaticResource NewPasswordBoxStyle}"
                             helpers:WatermarkHelper.WatermarkText="Repet your Password"
                             Margin="10 6 10 12"
                             PasswordChanged="PasswordBox_PasswordChanged" />
                <StackPanel Orientation="Horizontal"
                            Margin="12 6">

                    <!-- Cancel Button -->
                    <Button x:Name="ActionButton"
                            Content="{Binding ButtonText}"
                            Width="180"
                            Height="35"
                            Margin="6 0"
                            HorizontalAlignment="Center"
                            Command="{Binding ButtonCommand}"
                            Style="{StaticResource SecondaryButtonStyle}" />

                    <!-- Submit Button -->
                    <Button x:Name="CancelButton"
                            Content="{Binding SubmitButtonText}"
                            Width="180"
                            Margin="6 0"
                            Height="35"
                            HorizontalAlignment="Center"
                            Command="{Binding SubmitCommand}"
                            Style="{StaticResource PrimaryButtonStyle}" />
                </StackPanel>


            </StackPanel>

        </Grid>
    </Border>

</UserControl>
